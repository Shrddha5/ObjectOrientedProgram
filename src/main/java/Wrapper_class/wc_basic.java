package Wrapper_class;

public class wc_basic {
    //wrapper class i.e. A wrapper class is a simple Java class that "wraps"
    // or holds a basic data type (like int, char, or boolean) inside an object.
    // This lets you treat simple values as full-fledged objects when needed,
    // such as in collections like ArrayList.

}
class  Old{
int a;// its not wrapper class
}

class New{
    Integer a;//its wrapper class
}
// in realproj we always use wrapper class data type which is below
/**
 * Common Examples
 * int → Integer
 *
 * char → Character
 *
 * boolean → Boolean
 *
 * double → Double
 */